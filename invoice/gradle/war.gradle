apply plugin: "war"

bootWar {
    mainClass = "com.mycompany.store.InvoiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
